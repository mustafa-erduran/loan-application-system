version: '3'

services:

  loan-app-db:
    image: mysql:8.0.17
    environment:
      MYSQL_USER: root
      MYSQL_ROOT_PASSWORD: root123
      MYSQL_DATABASE: loandb
    ports:
      - "3306:3306"
 